DB_USER=root
DB_PASSWORD=tu_clave_local
DB_HOST=localhost
DB_PORT=3306
DB_NAME=sistema_ventas
AUTH_SECRET_KEY=cambia_esta_clave_por_una_larga_y_segura
PUBLIC_API_URL=http://localhost:8000
ENV=dev

# Admin bootstrap (dev/prod)
ALLOW_DEFAULT_ADMIN=false
INITIAL_ADMIN_PASSWORD=cambia_esta_contrasena

# Redis (rate limiting, caching)
REDIS_URL=redis://localhost:6379/0
ALLOW_MEMORY_RATE_LIMIT_FALLBACK=true

# SaaS / Trial
TRIAL_DAYS=15

# Logging
LOG_LEVEL=INFO

# DB pool tuning
SQLALCHEMY_POOL_SIZE=5

# Marketing analytics (landing /sitio)
# Google Analytics 4 Measurement ID
GA4_MEASUREMENT_ID=

# Meta/Facebook Pixel ID
META_PIXEL_ID=

# Stress tests (separate DB to avoid data conflicts)
# STRESS_DB_URL=mysql+aiomysql://root:pass@localhost:3306/sistema_ventas_stress
