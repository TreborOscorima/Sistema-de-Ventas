*.db
.states
*.py[cod]
assets/external/
assets/exports/
.web
.web/
.states/
reflex.lock/*
# Excepción: versionamos los lockfiles persistidos de Reflex para builds
# reproducibles (y para que el override de es-toolkit ^1.48.0 —fix del bug de
# recharts/compat CJS— llegue a producción). Ver scripts/docker-entrypoint.sh.
!reflex.lock/package.json
!reflex.lock/bun.lock
__pycache__/
/app/__pycache__/
.env
logs/
*.log
backups/
.deploy_prev_commit
mysql_data/
redis_data/
.claude/
.clone/

# Artefactos de testing/dev
.coverage
dev.err
e2e_screenshots/
testing_session_log.md

# _vendor/ ya no se usa para builds (tuwayki-core se instala vía requirements.txt,
# GitHub SHA). Se mantiene ignorado por si se clona el core localmente para dev editable.
_vendor/

# Material de marketing / documentos no versionados
*.docx

# Volcados accidentales de salida (ej. `... 2>0`)
/0
